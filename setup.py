from setuptools import setup

setup(
    version = '0.0',
    namespace_packages = ["mrbavii"],
    name = 'mrbavii.pysite',
    description = 'A set of Python help classes for WSGI applications',
    install_requires = ['setuptools']
)

